import { MyTableDirective } from './my-table.directive';

describe('MyTableDirective', () => {
  it('should create an instance', () => {
    const directive = new MyTableDirective();
    expect(directive).toBeTruthy();
  });
});
