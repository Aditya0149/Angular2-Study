import { SelectHostDirective } from './select-host.directive';

describe('SelectHostDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectHostDirective();
    expect(directive).toBeTruthy();
  });
});
